# NFC Audio Publisher

将多个 MP3 批量生成独立的 GitHub Pages 播放页，供 NFC 标签写入。

## 固定流程

1. 将 MP3 放入 `inbox`，文件名使用 `01_标题.mp3`、`02_标题.mp3`。
2. 双击 `publish.cmd`。
3. 从自动生成的 `nfc-links.txt` 复制网址并写入对应 NFC 标签。

编号决定永久网址。以后替换音频时保持相同编号，NFC 中的网址无需改写。

手机浏览器通常禁止无操作自动播放有声媒体，因此页面会尝试自动播放，并在被拦截时提供播放按钮。
